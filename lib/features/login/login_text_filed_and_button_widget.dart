import 'package:flutter/material.dart';



class loginTextFiledAndButtonWidget extends StatelessWidget {
  const loginTextFiledAndButtonWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      // children: [Text('data'), AppTextFiled(), AppTextFiled(), AppTextFiled()]
    );
  }
}